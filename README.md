# matchify
SwipeTune is a collaborative playlist app where users build playlists together using a Tinder-like swipe system. Songs are automatically added based on group voting. Built with React Native, Rust (GraphQL), and MongoDB.
